#include <bits/stdc++.h>

using namespace std;


int main(){
    int n;
    scanf("%d", &n);

    for(int nn = 0; nn < n; nn++){
        int x0, y0;
        int x1, y1;
        int x2, y2;
        int x3, y3;

        for(double theta1 = 0; theta1 < PI; theta1 += 0.0001){







        }

    }

    return 0;
}
